enpal_assessment_project:
  outputs:
    dev:
      dbname: postgres
      host: localhost
      pass: admin
      port: 5432
      schema: pipedrive_analytics
      threads: 1
      type: postgres
      user: admin
  target: dev
